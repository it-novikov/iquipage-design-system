> Исторический документ R2/R3. Текущий запуск и статус описаны в RELEASE-R4.md и evidence/final/verification.json. Старые ограничения браузерного прогона не являются статусом R4.

# Фактическая доставка и checkpoints

Полный исходный код, тесты, интерфейс, reference-сервер и документация находятся в архиве `IQUIPAGE-Maps-Implementation-R2.zip`. Доставка полной реализации в GitHub **не завершена**: часть операций записи коннекторов заблокирована сообщением «не смогли определить статус безопасности запроса». Эти отказы не были выданы за успешный push.

## Подтверждённая ветка GitHub

Репозиторий: `it-novikov/iquipage-design-system`.
Ветка: `feature/maps-platform-module`.
Main остаётся `1932c2464e695ac7d7e020f2551317943d78a73f` на момент последней подтверждённой проверки.

Успешные checkpoints:

- `c2cbafe060dcf4d7edd877da665b7c1b95340362` — scope реализации.
- `da3f718ce4c97ae764cfc319812d7d63c9ed2e1e` — отдельный DS source candidate, embedded API, сборка и границы.
- `9348d09f162bb77fe08eb0946e6534d3f806975d` — common.js.
- `8f4b3e496f1d78296294f22ac538fa42908d2754` — document.js.
- `c0b5c1b809ccca18e2244a729285210c0a6ab74f` — честный статус доставки и границы тестирования.
- `19552685138bde11efa31f125b7d25b219cb09d3` — ранняя полная модель жизненного цикла model.js; синтаксис проверен, push подтверждён.

- `30abc415ffc81d8ba280148c169f1cd9ff41cf6d` — запись дополнительного checkpoint и границы полной поставки.

Последний перечисленный commit — последний подтверждённый HEAD ветки. Полный UI/reference runtime из архива туда не попал. Архив содержит последующие исправления, в том числе расширенную валидацию; файлы могут отличаться от checkpoint даже при одинаковом имени.

## Подтверждённая копия на Mac

`/Users/nvkv71/iquipage-work/maps-implementation-20260910`

В эту новую отдельную копию выполнен pull checkpoints и создан/отправлен дополнительный коммит модели. Исходная дизайн-система не менялась. Для DS-checkpoint выполнена успешная официальная сборка candidate на Node 26.0.0. Это проверка сборки DS, не browser-run нового модуля.

Дополнительно через Remote Desktop Commander записан полный ранний `packages/maps/src/model.js`, затем он закоммичен и отправлен (`1955268`); также записано начало `src/repository.js`. Последующий append repository.js был заблокирован, поэтому этот локальный файл неполон. Он не был закоммичен как завершённая реализация. Не использовать текущую Mac-папку как полную поставку без переноса файлов из архива и проверки git diff.

## Перенос полной поставки

Следующий разрешённый исполнитель должен сопоставить архив с рабочим деревом, сохранить любые новые пользовательские изменения, перенести полный `iquipage-maps/` в `packages/maps/`, не переносить `.dev-state` и не менять `design-system/`, выполнить build/test/browser-run и сделать обычные коммиты в feature-ветку. Force push, main merge, npm publish и deploy этой задачей не выполнялись.
