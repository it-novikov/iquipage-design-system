--- 1.0/skills/iquipage-discovery/SKILL.md
+++ 1.1/skills/iquipage-discovery/SKILL.md
@@ -1,61 +1,43 @@
 ---
 name: iquipage-discovery
 description: "Проводит problem-first discovery: уточняет реальную проблему и ожидания, собирает бизнесовые, пользовательские, функциональные и нефункциональные требования, роли людей и AI-агентов, ограничения и критерии успеха. Использовать до проектирования новой функции или при неясном запросе. Предлагает решение и краткий бриф для явного утверждения; не начинает реализацию и не навязывает функцию, которую пользователь назвал первым вариантом."
-compatibility: "Агент с чтением файлов и кода. Python 3.10+ для помощников; браузер/Playwright для runtime-приёмки. IQUIPAGE предоставляется отдельно. Без автоматического сетевого доступа или deploy."
+compatibility: "Чтение материалов и диалог с пользователем. DS нужен для подтверждения реализуемости, не для формулировки проблемы. Скилл не предоставляет инструменты или полномочия."
 metadata:
-  suite-version: "1.0.0"
+  suite-version: "1.1.0"
   ds-baseline: "0.5.7"
   language: "ru"
 ---
 
+# Problem-first discovery
 
-# Discovery: сначала проблема, потом решение
+## Начать с известного
+Сформулируй проблему, требования и минимально достаточное решение; выдай краткий бриф для явного утверждения. Код пока не писать. [Рабочий контракт](references/execution-contract.md) определяет полномочия и продолжение, [конституция](references/constitution.md) — сохранение языка IQUIPAGE.
 
-## Задача
-Помоги сформулировать проблему, собрать необходимые требования и выбрать минимально достаточное решение. Выдай короткий бриф для явного утверждения. Не начинай реализацию до утверждения и не выдавай пользовательскую догадку о UI за доказанную потребность.
+Сначала прочитай доступный issue/brief/исследования/согласования. Отдели факт, предположение и неизвестное. **Не спрашивай повторно известное и не начинай с 3–5 вопросов автоматически.** Если для решения уже достаточно данных — сразу дай краткий proposed brief. Если нет — задай 1–3 самых важных вопроса, максимум пять за раунд. DS не обязателен для исследования проблемы; без него совместимость решения остаётся непроверенной.
 
-Прочитай [конституцию](references/constitution.md) и [human-agent](references/human-agent.md). При проверке реализуемости — [release map](references/release-map.md) и [gap protocol](references/gap-protocol.md). Углубляйся в `references/interview-guide.md` по необходимости, не задавай весь справочник вопросов за один раз.
+## 1. Один реальный эпизод
+Кто, чего хотел, где и что произошло вместо, как решает сейчас, последствия/частота. Недавний конкретный пример полезнее общей оценки. Причину помечай гипотезой, пока она не подтверждена. Разделяй проблему, симптом и названную первым вариантом функцию.
 
-## 1. Прочитай уже известное
-Используй brief/issue/repository/приложенные исследования и прошлые согласования. Зафиксируй «подтверждено», «предположение», «неизвестно». Не проси повторно данные, которые доступны. Не выдумывай рынок, сроки, выручку, бюджет, аудиторию или метрики.
+Формула: «Роль в контексте не достигает результата из-за наблюдаемого барьера; последствия; обход сейчас». Не выдумывай рынок, бюджет, аудиторию, частоту и метрики. В `references/interview-guide.md` — дополнительные вопросы только по необходимости.
 
-Первое сообщение — 3–5 самых важных неповторяющихся вопросов. Уважай формат пользователя; один вопрос, если ответ может радикально изменить всё. Без технического жаргона, с понятным примером. Не превращай разговор в анкету из 40 пунктов.
+## 2. Требования короткими раундами
+Покрой релевантное: бизнесовую ценность/ограничения; роли/навыки/устройства; product scope/сущности; функции/альтернативные пути; безопасность/данные/сохранность; нагрузку/latency/доступность; миграцию/паритет. Для команды агентов — мандат, данные, лимиты, принятие, отмена и [контракт действий](references/human-agent.md).
 
-## 2. Сформулируй проблему на конкретном эпизоде
-Кто столкнулся, в какой ситуации, чего хотел добиться, что произошло вместо, как решает сейчас, каковы последствия и частота. Попроси один недавний реальный пример, если его нет. Раздели проблему, симптом и предполагаемый способ лечения.
+После каждого ответа обнови понимание и только затем выбери следующий вопрос. Числовые targets без согласования — proposed/TBD. Референсы опциональны: уточни, что полезно и что не переносить. Отсутствие референса не блокирует исследование.
 
-Формула: «[Роль] в [контекст] не может/тратит [усилие] на [результат], потому что [наблюдаемый барьер]; это приводит к [последствие]. Сейчас используется [обход]». Причину помечать гипотезой, пока не подтверждена.
+У requirements — ID (BIZ/USR/FUN/NFR/AGT/DS), source, priority, owner и проверяемое acceptance. Для маленькой задачи достаточно короткой таблицы, не всех пустых регистров. Если пользователь не знает технический параметр, предложи гипотезу проверки, не заставляй его придумывать число.
 
-## 3. Собери требования короткими раундами
-После каждого раунда кратко отрази понимание, измени открытые вопросы и выбери следующий самый ценный. Остановись, когда есть достаточно для решения; не требуй точных бюджетов там, где сначала нужен эксперимент.
+## 3. Достаточно ли данных и нужна ли функция
+Готовность к предложению: понятны роль, проблема, желаемый результат, важные ограничения и кто принимает решение. Неизвестность, которая меняет scope/права/стоимость, блокирует утверждение, а не заполняется уверенным текстом. Остальные unknowns можно вынести в план проверки.
 
-Покрой релевантное:
-- бизнес: зачем, ценность/риски, ограничения, владельцы решения, ресурсы, сроки и trade-offs;
-- пользователи: роли, навыки, частота, устройства, доступность, язык, контекст совместной работы;
-- продукт: рабочие объекты, существующие маршруты, момент входа, ценность и expected outcome, зависимости, scope/out of scope;
-- функции: основные/альтернативные сценарии, данные, поиск, создание/редактирование, permissions, ошибки, undo/retry, real persistence;
-- нефункциональное: latency и объём данных с единицами/условиями, надёжность, восстановление, offline, security/privacy/retention, accessibility, поддержка браузеров, наблюдаемость, стоимость;
-- агенты: типы исполнителей, кто делегирует, мандат/лимиты, кто принимает результат, audit/provenance, cancel, безопасный повтор, запрещённые действия;
-- миграция: что нельзя потерять, обратная совместимость, cutover/rollback, принятые DS-паттерны.
+Сравни улучшение существующего пути, более простое content/process решение и новую функцию, когда эти альтернативы осмысленны. Не создавать три искусственные альтернативы для точечной правки. Возможный результат — не делать новую функцию.
 
-Для каждого требования назначь ID (`BIZ`, `USR`, `FUN`, `NFR`, `AGT`, `DS`), source, priority, owner и проверяемый acceptance. Численные значения без ответа владельца обозначать proposed/TBD. Референсы запроси при наличии: что именно полезно и что не переносить. Не требуй референс как условие начать исследование.
+Совместимость сверять по [реальному релизу](references/release-map.md), когда он доступен. Missing API не представлять существующим; по [GAP protocol](references/gap-protocol.md) записать риск/внешнюю зависимость. Не урезать известные функции из-за состава DS.
 
-## 4. Проверь необходимость и реализуемость
-Рассмотри минимум: улучшить существующий путь; лёгкое procedural/content решение; новая функция. Можно выбрать «не делать новую функцию». Оцени пользу/стоимость/риски/unknowns, не рассчитывай ROI из выдуманных чисел.
+## 4. Предложение и утверждение
+Бриф примерно 250–500 слов: проблема, аудитория, результат, решение, главный сценарий, входит/не входит, сохранение функций, DS fit/gaps, ограничения и риски. Подробности отдельно в `assets/requirements-register.md`; форма — `assets/solution-brief.md`.
 
-Сопоставь рекомендуемую композицию с реальными возможностями DS. Не обсуждай недокументированный API как готовый. Missing component/capability оформить до утверждения как риск/внешнюю зависимость. Подтверждённые функции/права продукта нельзя урезать ради доступных компонентов.
+Заверши одним вопросом об утверждении конкретной revision/scope. Proposed остаётся proposed до однозначного ответа. Молчание/«интересно»/собственный вывод не approval. Уже данное ясное разрешение не спрашивать снова. Существенное изменение функций/прав/риска требует нового решения, мелкое уточнение принятого — нет.
 
-## 5. Спроектируй предложение, не код
-Дай рекомендованный путь пользователя и agents, основные сущности и состояния, структуру экрана, критические ошибки и восстановление, место человека в подтверждении. Отдельно опиши, как будет ясно, что проблема решена. Для важных unknowns предложи дешёвый прототип/исследование, а не дорогую полную реализацию.
-
-Сформируй `assets/solution-brief.md`: примерно 400–700 слов или одна читаемая страница, без технической простыни. Включи проблему, аудиторию, успех, решение, ключевой сценарий, scope/out of scope, DS-fit/gaps, ограничения/риски и вопросы на утверждение. Детальные requirements и decision log — отдельно.
-
-## 6. Получи настоящее утверждение
-Заверши одним понятным вопросом: «Утверждаем этот объём и сценарий, или нужно изменить …?» Статус brief остаётся `proposed`, пока нет явного ответа владельца. Не считать молчание, «интересно», отсутствие возражений или собственный вывод согласием. Уже данное однозначное разрешение не запрашивать заново; сохранить его точный scope и reference.
-
-При ответе внеси изменение, подними revision, сохрани approver/date/message reference. Существенное изменение scope/permissions/risk после approval требует повторного согласования; небольшие уточнения реализации в уже одобренных границах — нет.
-
-После утверждения передай в `iquipage-implement`: brief, requirements register, acceptance, evidence/assumptions, decision log, unresolved gaps и rollout boundary. Approval решения не означает разрешение на публикацию, deploy или создание отсутствующей DS.
-
-## Не подменяй работу
-Не начинай строить названный пользователем экран без понимания результата. Не обещай исследования с участниками, если их не проводил. Не скрывай противоречия требований и не выбирай за владельца бизнес-приоритет. Не устраняй неопределённость уверенным тоном: покажи её и предложи следующую проверку.
+## Передача
+Сохрани brief, requirements, sources/assumptions, decision reference, gaps, acceptance, run-state. Только после approval передай исполнителю. Согласование дизайна не разрешает деплой, изменение vendor, платежи или публичную публикацию. Если реализация не поручена, завершить на брифе; не переходить к коду автоматически.
--- 1.0/skills/iquipage-experience-audit/SKILL.md
+++ 1.1/skills/iquipage-experience-audit/SKILL.md
@@ -1,56 +1,41 @@
 ---
 name: iquipage-experience-audit
 description: "Аудирует существующие UI/UX и продуктовые сценарии в концепции IQUIPAGE: ясность, информационная архитектура, роли людей и AI-агентов, визуальный шум, сетка, плотность и путь к результату. Использовать для разбора неудобного интерфейса и плана концептуальной пересборки. Выдаёт evidence-based отчёт и backlog переделок. Не меняет код без разрешения и не выдаёт экспертный walkthrough за пользовательское исследование; кодовую приёмку выполняет iquipage-review."
-compatibility: "Агент с чтением файлов и кода. Python 3.10+ для помощников; браузер/Playwright для runtime-приёмки. IQUIPAGE предоставляется отдельно. Без автоматического сетевого доступа или deploy."
+compatibility: "Чтение проекта и принятой DS. Помощники: Python 3.10+; browser capture отдельно требует Playwright и установленный движок. Ограниченный аудит возможен без runtime, но без его приёмки."
 metadata:
-  suite-version: "1.0.0"
+  suite-version: "1.1.0"
   ds-baseline: "0.5.7"
   language: "ru"
 ---
 
+# Аудит понятности и целостности продукта
 
-# UI/UX-аудит: от рабочего вопроса к понятному результату
+## Вход и граница
+Разбери опыт человека и релевантные агентные сценарии, предложи конкретную перестройку. Не меняй приложение без поручения. Сохрани функции и принятый язык IQUIPAGE.
 
-## Граница задачи
-Исследуй фактический опыт и предложи конкретную перестройку. Не ограничивайся цветами и не своди аудит к списку компонентных багов. Код не меняй, пока это отдельно не поручено. Сохрани принятый язык IQUIPAGE и функции продукта; новый feature scope требует утверждения.
+Начни с [конституции](references/constitution.md) и [рабочего контракта](references/execution-contract.md). Прочитай жалобы, текущий UI, маршруты, прежние решения и доступные данные. Укажи доступный уровень: кадр / код / interactive walkthrough / наблюдение участников. Нельзя превратить один screenshot в доказательство неудобства всех сценариев.
 
-Прочитай [конституцию](references/constitution.md), [human-agent](references/human-agent.md), [метод доказательств](references/quality-method.md), [release map](references/release-map.md) и [gap protocol](references/gap-protocol.md). Используй `assets/experience-report.md`, `assets/journey-matrix.csv` и `assets/agent-action-contract.md`.
+## 1. Рабочая задача
+Кто, в каком контексте и как часто делает что; где начинает, какой результат нужен, как узнаёт, что он сохранён; что происходит при отказе. Если неизвестность меняет решение, спроси только блокирующее. Если brief уже достаточен, не запускай повторный опрос. Discovery нужен для новой проблемы, не как обязательный церемониал.
 
-## 1. Определи основу и уровень уверенности
-Прочитай бриф, реальную навигацию и модели, пользовательские жалобы, аналитику/записи с доступом и предыдущий audit. Снимок не доказывает поведение. Пользовательское пожелание не доказывает частоту проблемы. Гипотеза агента не observation. Для каждого вывода сохраняй evidence/source и confidence.
+## 2. Пройди релевантные пути
+Первое полезное действие, повторение частой операции, поиск/продолжение, ошибки/отмена/возврат. Делегирование, проверка и принятие результата — только если продукт их поддерживает или они утверждены. Различай роли, доступ и отсутствие данных. Не путай свободную доску с редактором workflow.
 
-Если цель или роль неясна, задай 2–4 блокирующих вопроса либо используй `iquipage-discovery`. Не требуй полный опрос, если задача уже описана. Если доступен только screenshot, выдай ограниченный visual audit и список нужных поведенческих проверок, а не полный UX verdict.
+Записывай наблюдения отдельно от предполагаемых причин и эстетических предпочтений. Аналитика или заявка описывает источник, но не доказывает неизвестную частоту. При отсутствии runtime сформулируй ограничения и необходимые поведенческие тесты.
 
-## 2. Составь карту опыта
-Инвентаризируй поверхности и отношения рабочих сущностей. Выдели реальные jobs и частоту, новичка/эксперта/владельца/ограниченную роль/агента. Для каждого пути: trigger, исходные сведения, задача, шаги, трудность, доказательство результата, отказ, восстановление.
+## 3. Три перспективы, не выдуманная команда
+UX-исследование: расхождение с ожиданиями; гипотезы; задания для пользователей без подсказок жестов. UI/UX: информационная архитектура, сценарии, ошибки, раскрытие сложности и доступность. Арт-дирекция: [главный объект, иерархия, сетка, ритм и выразительность](references/design-review.md). Перечисление перспектив не означает интервью или работу нанятых специалистов.
 
-Пройди минимум: первое полезное действие; повторение частой операции; найти и продолжить работу; делегировать агенту; проверить/принять результат; отказ и возврат. Выбирай релевантные сценарии продукта, не добавляй агентную фичу туда, где её нет. Не путай whiteboard-брейншторм с редактором исполнения процесса.
+Для человеческих и агентных ролей используй [human-agent](references/human-agent.md): стабильные сущности, мандат, явные последствия, проверяемое принятие, отмена, audit trail. Машинная доступность не отменяет читаемые подписи и не даёт агенту дополнительных прав.
 
-## 3. Три аналитические перспективы
-**UX-исследование:** где поведение/термины расходятся с ожиданиями; что реально подтверждено; какой вопрос проверить с участниками. Не придумывай интервью и статистику. Предложи usability tasks без подсказок жестов, критерии успеха, группы, метрики, согласие и безопасные данные.
+## 4. Конкретное целевое решение
+Для каждой проблемы: текущее представление → препятствие результату → что объединить/перенести/убрать/переименовать → новый путь → supplied DS pattern → acceptance. «Сделать премиальнее» без конкретной структуры не задание.
 
-**UI/UX-проектирование:** путь, узнаваемость actions, progressive disclosure, навигация, контекст, ошибки, meaningful defaults, сохранность и клавиатура. Оцени потребность вообще в новой функции и возможность упростить процесс вне интерфейса.
+Пример: соединить header/summary через public compact work layout, перенести свойства задачи из основного потока в контекст, сопоставить полезную рабочую площадь при 1280×720. Это гипотеза композиции до проверки, не обещание роста конверсии.
 
-**Арт-дирекция:** основной объект, визуальная иерархия, масштаб, сетка, плотность, типографика, согласованность, ограниченные акценты, поведение света/motion и качество материалов. Не смешивай вкусовую оценку с измеренным дефектом.
+Для существенного спорного решения сравни две альтернативы на одинаковых данных; для локального бага достаточно одной исправленной композиции. Проверяй mappings по [release map](references/release-map.md) и реальному API. Missing capability передать по [DS-GAP](references/gap-protocol.md), не как задание «сделать похожий контрол».
 
-Отдельный реальный рецензент полезен, если доступен; его отсутствие честно обозначить. Перечисление перспектив не означает работу нанятой команды.
+## Выход и следующий шаг
+Выдай короткое резюме, карту путей, конкретный backlog с ID, evidence/confidence, priority, целевой структурой, DS mapping и проверкой. Используй `assets/experience-report.md`; для исследований — вопросы и критерии, не фиктивные результаты. Предложение сначала утверждается, если меняет продуктовый scope.
 
-## 4. Проверяй человека и агента на одном процессе
-Человек понимает где он, что происходит, кого затронет действие и как отменить. Агент получает стабильные IDs, разрешения, понятные контракты состояний и проверяемый результат. UI не требует знания внутреннего backend; API не должен полагаться на случайные подписи и координаты.
-
-Проверить роли, автора и инициатора; scope/лимиты; intent → preview → execution → review → accepted; cancel/retry/conflict; provenance артефакта. Уточнение и отказ не выглядят как завершение. Не требовать скрытую chain-of-thought. Авторизация, аудит и исполнение — требования приложения, а не свойства, которые нужно выдумать в DS.
-
-## 5. Сформируй целевую композицию
-По каждому confirmed issue опиши: что видит пользователь сейчас → почему это мешает цели → что убрать/объединить/перенести/переименовать → новый путь → подходящие supplied patterns → acceptance. Пример: «Объединить project header и summary в compact work layout, оставить описание задачи главным потоком, свойства — в контекстной области; сравнить первые рабочие строки на 1280×720». Не писать «добавить воздуха» без места/меры/цели.
-
-Для спорного крупного решения дать 2 альтернативы с trade-offs на одинаковых данных, выбрать рекомендуемую как гипотезу. Показывать не только happy state, но и no data/denied/error/long content. Изменения workflows и модель ролей — предмет согласования.
-
-Каждый фрагмент mapping проверяется по реальному API. Отсутствующая возможность — DS-GAP, не задание frontend-агенту «сделать похожее». Публичная composition допустима; новая интерактивная механика нет.
-
-## 6. Приоритизируй и передай исполнителю
-Раздели: критические функциональные/доступностные барьеры, системная архитектура, локальные проблемы удобства, эстетический polish. Для каждого finding — surface/journey/actor/evidence/impact/target change/DS mapping/test/owner. Не ранжируй только визуальную заметность.
-
-Выход: краткий verdict; journey map; before/target описание; backlog с устойчивыми ID; gap register; вопросы владельцу; plan validation with users; критерии приёмки и handoff в `iquipage-implement`. Для bugs реализации передай также в `iquipage-review`. Укажи, какие выводы — гипотезы и какие наблюдения отсутствуют.
-
-## Что не делать
-Не удалять технически сложные функции ради чистого скриншота. Не обосновывать качество модными референсами. Не показывать все состояния одновременно в основном UI. Не превращать агента в неразличимого человека или всесильного пользователя. Не называть обзор красивых страниц тестом всей платформы. Не присваивать полный ready по одному audit.
+Для реализации передай `iquipage-implement`; для дефектов кода — `iquipage-review`. Если их нет, отдавай переносимый handoff, не утверждай запуск. Сохранить run-state и исходные ограничения. Нет итогового «идеально»: оценка касается явно просмотренного scope, а не всех возможных людей и состояний.
--- 1.0/skills/iquipage-implement/SKILL.md
+++ 1.1/skills/iquipage-implement/SKILL.md
@@ -1,67 +1,48 @@
 ---
 name: iquipage-implement
 description: "Внедряет IQUIPAGE как полную систему UI/UX: композиция, структура, поведение, состояния и публичные компоненты, а не только темы и токены. Использовать для миграции интерфейса, реализации утверждённого дизайна или исправления замечаний аудита. Сначала проверяет поставку и соответствие функций, сообщает о missing components/capabilities, не создаёт локальные замены DS. Не применять вместо discovery для неопределённой проблемы или вместо read-only ревью."
-compatibility: "Агент с чтением файлов и кода. Python 3.10+ для помощников; браузер/Playwright для runtime-приёмки. IQUIPAGE предоставляется отдельно. Без автоматического сетевого доступа или deploy."
+compatibility: "Чтение проекта и принятой DS. Помощники: Python 3.10+; browser capture отдельно требует Playwright и установленный движок. Ограниченный аудит возможен без runtime, но без его приёмки."
 metadata:
-  suite-version: "1.0.0"
+  suite-version: "1.1.0"
   ds-baseline: "0.5.7"
   language: "ru"
 ---
 
+# Концептуальное внедрение IQUIPAGE
 
-# Полное внедрение IQUIPAGE
+## Результат и первый шаг
+Пересобери ВЕСЬ согласованный UI: композицию, состояния, поведение и публичные компоненты. Сохрани функции, данные и права. Утверждённую удачную композицию не меняй ради демонстрации объёма работы. «Все возможности библиотеки» — рассмотреть её возможности для каждого сценария, не заставлять пользователя видеть все компоненты.
 
-## Результат и граница
-Пересобери весь согласованный UI на существующей IQUIPAGE, сохранив предметную семантику и функции. Не ограничивайся skinning. Не меняй продуктовые правила и DS без решения владельца. «Все возможности» означает рассмотреть весь каталог и выбрать нужное каждому сценарию, а не показать все компоненты.
+Сначала прочитай [конституцию](references/constitution.md) и [рабочий контракт](references/execution-contract.md). Найди проект, принятый DS, scope и предыдущий run-state. Выбери точечную работу или полную миграцию. Не проводи discovery заново для ясных согласованных исправлений.
 
-Прочитай [конституцию](references/constitution.md), [карту поставки](references/release-map.md) и [протокол пробелов](references/gap-protocol.md). Для сценариев агентов используй [human-agent](references/human-agent.md). Для приёмки — [метод качества](references/quality-method.md). Пути скриптов разрешай от каталога этого SKILL.md, а не от cwd проекта.
+## 1. База и паритет до кода
+Прочитай действующие инструкции проекта, команды и авторизацию. Сохрани чужие незакоммиченные изменения. Проверь реальную поставку через `scripts/verify_release.py`; [release map](references/release-map.md) и `assets/ds-release-lock.json` относятся к pinned 05.7, не к любому latest. Не меняй pin для зелёного результата. Для принятой новой DS нужен отдельный provenance review и новый lock.
 
-## 1. Установи базу до правок
-Найди репозиторий, неизменённый DS-архив, одобренный scope/бриф и текущие отчёты через доступные чтение/search. Не проси повторно загрузить то, что можно прочитать. Если нужный источник отсутствует, назови конкретно его и заблокированное действие.
+Инвентаризация охватывает routes, auth, overlays, settings/admin, flags, mobile, empty/error/recovery и роли. Свяжи каждую существующую операцию с поверхностью, предметной сущностью, DS-контрактом и acceptance. Таблица вместо обязательного холста, дедлайн вместо duration, readonly вместо редактирования — потеря функции, пока пользователь явно не согласовал изменение.
 
-Зафиксируй repo revision + dirty state, текущий стек, целевые платформы, команды сборки, действующую авторизацию. Прочитай инструкции проекта перед запуском его команд. Не меняй несвязанные правки пользователя. Создай migration ledger из `assets/migration-plan.md`.
+## 2. Проектирование и DS mapping
+Примени [рубрику концепции](references/design-review.md). Главная работа, следующий шаг, служебные свойства и подтверждённый результат должны различаться. Используй общий shell/grid/density, а не коллекцию независимо оформленных страниц.
 
-Проверь пакет без исполнения его кода:
-```sh
-python3 SKILL_ROOT/scripts/verify_release.py --ds-root PATH_TO_DS --archive PATH_TO_ZIP --out WORK_DIR/ds-verification.json
-```
-`SKILL_ROOT`, `PATH_TO_DS`, `WORK_DIR` — заполнить установленными путями. Пин 05.7 поставлен в `assets/ds-release-lock.json`; несовпадение нельзя обходить изменением ожидаемого SHA. Новую одобренную поставку сначала исследовать и перепривязать явно. Известное несовпадение version literal в types фиксировать, не молча исправлять.
+Прочитай релевантные семейства [карты поставки](references/release-map.md) и `references/capability-index.json`, затем фактические exports/types/пример принятой DS. Индекс — навигация, не доказательство API. Подбери supplied component, supplied composition или ограниченный adapter.
 
-## 2. Инвентаризируй продукт, не только красивые маршруты
-Пройди route tree, auth, navigation, deep links, overlays, feature flags, settings/admin, error/recovery и скрытые действия. Запиши все поверхности в `assets/surface-inventory.csv`: роль, основной объект/задача, текущие функции, states, DS composition, статус миграции/проверки.
+При большой миграции сначала цельный вертикальный сценарий: shell → список → объект → изменение → API → reload/отказ/отмена, light/dark/узкий экран. Получи решение по НОВОЙ композиции, если оно ещё не дано. Распространяй проверенный паттерн, не слабый первый черновик.
 
-Не теряй: создание/редактирование/удаление, вложенность, bulk actions, фильтр/sort/page, импорт/экспорт, сохранённый вид, уведомления, результат агента, обсуждение, права, отмену и reload. Определи обязательные тестовые роли и данные. Не засчитывай неподключённый demo за существующую функцию.
+## 3. Недостаток DS — сообщить до обхода
+Используй [DS-GAP](references/gap-protocol.md) и `assets/ds-gap.md`. Разделяй: возможность есть; ошибка приложения; ошибка поставленной DS; новая общая возможность; необходимая platform adaptation. Приложи фактический export/тип/fixture и минимальный недостающий контракт. Блокируй затронутую часть, а не весь проект.
 
-При неясной проблеме запусти `iquipage-discovery`, если он установлен, либо его логику: уточни только блокирующее, подготовь краткий бриф и дождись утверждения. При очевидной корректировке по уже принятому отчёту не начинай опрос заново.
+Нельзя создавать новый контрол в app/shared и объявлять его DS, патчить внутренний DOM/CSS, импортировать private modules или ставить вторую UI-библиотеку. React event bridge и API adapter допустимы; новый календарь под видом bridge — нет. Native adaptation требует `assets/adaptation-record.md` с реальной платформенной причиной, публичным соответствием, владельцем и проверками.
 
-## 3. Спроектируй целевую систему экранов
-Определи информационную архитектуру вокруг рабочих объектов, не вокруг структуры API. Для каждого journey опиши: вход → основная работа → проверяемый результат → восстановление. Выдели первичное действие, secondary properties и progressive disclosure. У новичка путь не должен требовать знания runId/внутреннего workflow.
+## 4. Реализация и доказательства
+Один неизменённый vendor boundary; JS/CSS одной поставки. Используй поддерживаемые core/advanced/whiteboard entries и согласованный lazy budget. Не включай gallery, demos и example-state в продукт. Через props/events передавай модели; сохраняй прикладные поля и identity.
 
-Составь capability map **всех релевантных семейств** из `references/capability-index.json`, проверяя реальные props/events/docs. На каждый фрагмент выбери supplied component, supplied composition или обоснованный adapter. Маркетинговый, ежедневный, пространственный и reading режимы не смешивай.
+Проверь controlled state, IME, cleanup, abort, поздние ответы, повторный accept, конфликт revision, смену прав и черновики. Intent не равен записи. Для команд с агентами прочитай [human-agent](references/human-agent.md): мандат, исполнение, принятие и сохранение — разные вещи.
 
-Покажи до широкого внедрения один вертикальный сценарий на реальных/обезличенных данных: общий shell → список → объект → изменение → сохранение → отказ/отмена, в light/dark и узком виде. Утверди композицию, если она меняется за пределами уже согласованной. Не переименовывай доменные сущности ради валидации DS.
+## 5. Review → исправления → повторная проверка
+По [методу качества](references/quality-method.md) пройди реальный продукт, не gallery. Если доступен `iquipage-review`, передай scope, план и fingerprints; иначе явно self-review. Отдельный агент не гарантируется названием скилла.
 
-## 4. Останови локальное изобретение компонентов
-Для непокрытого сценария создай `assets/ds-gap.md`, приложи точный публичный контракт и минимальное воспроизведение. Сообщи пользователю до реализации обхода. Разрешённую альтернативу запиши с влиянием на функциональный паритет. Не называй весь проект заблокированным, когда пробел локален.
+Сначала воспроизвести finding, исправить общий root cause, добавить regression, повторить затронутые состояния на текущем build. Не менять assertions ради PASS. DS-дефект снова проходит GAP. Новый scope не появляется тайком в исправлении.
 
-Adapter может связывать state/events/framework lifecycle, нормализовать данные и использовать нативные platform services. Он не может перерисовывать календарь, popup, canvas, error state или кнопку вне supplied pattern. Не использовать private selectors, shadowRoot patches, DOM replacement или !important. Не ставить другую UI-library без запроса.
+## Доставка
+Для полной миграции: inventory, capability-map, parity, gaps/adaptations, evidence, assessment schema 2.0 и handoff. Для точечной — компактная запись тех же решений без пустой бюрократии. Скрипты scanning/fingerprint помогают собрать данные, не принимают UI. Сохрани `assets/run-state.example.json` для продолжения.
 
-## 5. Внедряй один общий источник оформления
-Подключи единый неизменённый vendor boundary. Используй `core`, а `advanced`/`whiteboard` загружай при открытии соответствующей функции. Full entry допустим только при обоснованном отсутствии lazy budget. CSS и JS одной выдачи; не включай catalog, stories и демонстрационное state в app runtime.
-
-Передавай модели свойствами. В framework adapter подписывайся на public events, снимай слушатели, отменяй pending requests, освобождай URL. Не допускай controlled event loops, поздних accept после cancel и потери разрешённых прикладных полей. Событие intent не равно подтверждённой записи; UI отражает реальный API outcome.
-
-Сначала общий app shell/layout/header/navigation/density, затем все согласованные поверхности через один mapping. Не оставляй старые uncontrolled стили в overlays, auth, settings или mobile. Изменяй business logic только по утверждённой необходимости и с tests. Названия, даты и строки растут по содержимому, не обрезаются для фиктивного равенства.
-
-## 6. Пройди цикл независимого ревью
-Запусти `iquipage-review` на всей финальной поверхности/диффе. Если отдельный агент недоступен, проведи явно обозначенный self-review, не выдавай его за независимое. Дополнительно используй `iquipage-experience-audit` для человеческих сценариев, если менялась композиция.
-
-Преобразуй каждую подтверждённую находку в correction task с ID. Исправь root cause, повтори affected cases и соседние контексты. Новый визуальный компонент, обнаруженный при исправлении, снова проходит DS-GAP, а не локальный workaround. Не закрывай defect изменённым ожиданием теста без объяснения изменения контракта.
-
-## 7. Выдай проверенный результат и честные границы
-По методу качества: все surfaces/scope, обе темы, нужные роли/permissions, keyboard/touch/zoom, combined states, реальный backend reload, сохранность черновика и прогресс. Сохрани before/after, конкретные измерения и реальные тесты на текущем fingerprint.
-
-Обнови migration ledger, mapping, gap register, adaptation records и handoff. Укажи завершённое, blocked, not run, принятые риски, команды и файлы. Гейт готовности проверяется `iquipage-review`, не количеством DS-imports. Не обещай «полностью внедрено», если хоть обязательная поверхность не пройдена. Не делай push/deploy без отдельного разрешения.
-
-## Выходные документы
-`migration-plan.md`, `surface-inventory.csv`, `capability-map.csv`, `gap-register/`, `adaptations/`, `implementation-handoff.md`, assessment/fix ledger от review. Пользователю — итог, реальные пути/архив, открытые блокеры, область тестов. Не изменять сам DS-пакет при установке этого скилла.
+Вердикт относится к согласованному scope, а не всей платформе по умолчанию. Не выдавай новую сборку с прежними доказательствами. Если запрошен архив, создай и проверь его перед ссылкой. Push/deploy — отдельное разрешение; полный проход этого скилла его не даёт.
--- 1.0/skills/iquipage-review/SKILL.md
+++ 1.1/skills/iquipage-review/SKILL.md
@@ -1,70 +1,52 @@
 ---
 name: iquipage-review
 description: "Проверяет реализацию и код внедрения IQUIPAGE: публичные компоненты, отсутствие локальных UI-замен, визуальное соответствие, сетка, отступы, контраст, состояния, доступность, производительность и сохранение функций. Использовать для приёмки, code review и цикла исправлений после внедрения. Выдаёт конкретные finding IDs и проверяемые способы исправления. По умолчанию read-only; не заменяет discovery или самостоятельное проектирование продукта."
-compatibility: "Агент с чтением файлов и кода. Python 3.10+ для помощников; браузер/Playwright для runtime-приёмки. IQUIPAGE предоставляется отдельно. Без автоматического сетевого доступа или deploy."
+compatibility: "Чтение проекта и принятой DS. Помощники: Python 3.10+; browser capture отдельно требует Playwright и установленный движок. Ограниченный аудит возможен без runtime, но без его приёмки."
 metadata:
-  suite-version: "1.0.0"
+  suite-version: "1.1.0"
   ds-baseline: "0.5.7"
   language: "ru"
 ---
 
-
-# Приёмка и исправления внедрения
+# Приёмка внедрения: код, реальный интерфейс и честные границы
 
 ## Режим
-По умолчанию исследуй без правок приложения. Если пользователь разрешил «исправь» или работа идёт внутри разрешённого implement/review-цикла, после фиксации отчёта исправляй root causes и перепроверяй. Не превращай review в необъявленный redesign.
+По умолчанию read-only для приложения; отчёты разрешены. В «проверь и исправь» или разрешённом implement-цикле сначала сохрани finding, затем выполняй правки в прежних границах. Не превращай review в незаказанный redesign.
 
-Обязательно прочитай [конституцию](references/constitution.md), [метод проверки](references/quality-method.md), [release map](references/release-map.md), [gap protocol](references/gap-protocol.md). Для агентных процессов — [human-agent](references/human-agent.md). Используй `assets/assessment.example.json` и `assets/findings-report.md`.
+Прочитай [конституцию](references/constitution.md), [рабочий контракт](references/execution-contract.md) и [метод качества](references/quality-method.md). Выбери: targeted audit или full-integration. Недоступный browser даёт ограниченный отчёт, не готовность runtime.
 
-## 1. Зафиксируй, что именно проверяешь
-Прочитай согласованный scope, текущий DS archive и hash, app revision/dirty diff, migration ledger, surface inventory, реальные test commands и ограничения среды. Проверить только изменённые файлы недостаточно: оценить все затронутые surfaces и shared components. Для полной приёмки нужен полный реестр маршрутов/overlays/ролей, не случайная выборка.
+## 1. Зафиксируй независимую основу
+Прочитай approved scope, inventories, DS archive/hash, app revision/dirty diff и план приёмки. Для всей миграции сверить все маршруты, overlays, auth/admin/flags/роли; не доверять списку только от исполнителя.
 
-Запусти read-only `scripts/verify_release.py`. Расхождение vendor — finding с точными файлами и хешами; не приводи поставку к baseline автоматически. Предыдущие успешные тесты — supplier evidence, а не результат этой приёмки.
+`verify_release.py` проверяет pinned DS. `fingerprint_inputs.py` может хешировать явно перечисленные app/build/lock/config inputs; полнота списка требует ревью. Прежние DS-отчёты — свидетельства поставщика, не новое тестирование продукта.
 
-## 2. Статическая разведка — только кандидаты
-```sh
-python3 SKILL_ROOT/scripts/integration_scan.py --root PROJECT --config WORK_DIR/scan-config.json --out WORK_DIR/scan.json
-```
-Конфигурация из `assets/scan-config.example.json`; пути source/vendor/adapters определить по реальному проекту. Скрипт ничего не меняет и не запускает код проекта. Не называй regex scan полноценным code review.
+До прогона сформируй `assets/acceptance-plan.example.json`: surfaces, required checks, dimensions, expected environment, метод и типы evidence. Подтверждённый digest хранится вне редактируемого assessment. Нельзя сократить план задним числом ради PASS.
 
-Разбери каждый сигнал вручную: native control в exact markup DS; правильная composition; bounded adapter; application integration; confirmed local bypass; unknown. Нет false-positive waiver «весь файл разрешён». Исключение с rule_id/file/line/source SHA/причиной/owner/approval — проверяемая заявка, не доказательство качества.
+## 2. Найди обходы и ошибки кода
+`scripts/integration_scan.py` — только кандидаты. Определи реальные roots/vendor/adapters, рассмотри исключения и пропущенные файлы. Ноль сигналов или exit 0 не доказывает соответствие DS. Каждое исключение должно иметь точную причину, owner, approval и SHA; адаптерная папка не разрешение на новый визуальный язык.
 
-Проверь не только новые компоненты, но и CSS modules, inline CSS, utility classes, CSS-in-JS, local SVG, portal content, auth forms, native date popup, внутренний DOM DS, старые UI-packages. Простые semantic HTML elements допустимы. Применение DS-токенов к самодельной кнопке не делает её supplied компонентом.
+Проследи import → rendering → event → state → API → confirmation → reload. Проверяй public props/types/events, slots, SSR при наличии, deep links, отмену, восстановление, race/conflict, permissions, безопасные URL/Markdown, cleanup/listeners/Blob. Разделяй supplied native HTML, правильную composition, interop, локальный bypass и unknown.
 
-## 3. Code review и публичные контракты
-Проследи импорт → rendering → input/event → state → API → подтверждение → reload. Проверить сохранность полей, типы данных, XSS/Markdown, unsafe URLs, утечки приватных media, listeners, AbortController, Blob cleanup, повторное/устаревшее подтверждение и revision conflict. Сопоставить event names с декларациями, не читать internal fields как контракт.
+Для подозреваемого DS-дефекта — минимальная fixture с неизменённым vendor без app styles. Не устраняй дефект private DOM-патчем: используй [GAP protocol](references/gap-protocol.md). Обязательную missing capability нельзя скрыть заменой функции.
 
-Разделить source of truth приложения и UI draft; не терять ввод при rerender, переключении темы/плотности/панели, смене permissions. Не заменять backend permission disabled-кнопкой. Проверить scopes людей и агентов, идентичность автора/исполнителя и принятие результата.
+## 3. Пройди интерфейс и профессионально оцени композицию
+Используй [рубрику](references/design-review.md). Проверяй главный объект, сетку, иерархию, density, icon boxes, отступы и переносы, orphan-разделители, визуальный шум, доступность actions. Сохрани и ПРОСМОТРИ before/after одинакового содержимого в обеих темах и согласованных размерах. Автоматический overflow не заменяет это действие.
 
-Проверить routes/deep links, semantic h1, slots, lazy entry graph, отсутствующие wrappers и demo state. Нативная адаптация только с доказанной необходимостью и bounded record. Для suspected DS-defect создать чистую fixture без app styles; если баг подтверждён в vendor, не патчить локально.
+Проверь meaningful data, happy/empty/loading/error/retry/denied/readonly/pending/conflict/cancel/undo/reload, selected+hover+focus-visible, вложенные dialog/menu/date, IME. Сравнивай фактическую составную поверхность после стабилизации анимаций. Genuine WebKit touch не заменяется Chromium mouse; CSS zoom не переименовывается в browser zoom.
 
-## 4. Реальная UI-приёмка
-Открой продукт в браузере, дойди до meaningful data, не только splash. Выполни ключевые сценарии в нужных ролях и обоих оформлениях. Составь матрицу по `assets/test-matrix.csv`, добавь риски проекта. Вращать галерею библиотечных образцов вместо продукта недостаточно.
+Пассивный `capture_surface.py` фиксирует кадр и измерения; не тестирует сценарий и не ставит PASS. Его сеть ограничивается точными разрешёнными origins. Используй обезличенные данные; URL-навигация исполняет JS и не является режимом sandbox.
 
-Сначала pointer и keyboard happy path, затем empty/loading/error/retry/denied/pending/conflict/cancel/undo/reload; selected+hover+focus; nested dialog/menu/calendar; mobile and zoom. Контраст меряй после стабилизации анимаций по составным фактическим поверхностям. Ошибка и warning, planned и in-progress различимы по смыслу и цвету.
+## 4. UX, агенты, производительность
+Новичок понимает действие и результат; эксперт сохраняет контекст. Для agent-сценариев [контракт](references/human-agent.md) отделяет инициатора, мандат, выполнение, принятие и сохранение. Там, где агентной функции нет, документировать применимость, а не выдумывать PASS.
 
-Скриншоты: минимум крупный/малый размер и обе темы для каждой изменённой композиции; реально просмотреть, не только сгенерировать. Оценить оси, группировку, иерархию, рабочую площадь, масштаб подписи, ритм, icon box, sticky headers, overflow, tooltip, focus, checkbox hit area и название целиком. Не заменять visual review вычислением scrollWidth.
+Измерь реальные bundle/network, lazy requests, idle, long tasks, большие рабочие модели и reduced motion в требуемой среде. Обозначь метод и данные. Размер gzip библиотеки не равен initial JS приложения, а JS-duration — не FPS.
 
-Пассивный `scripts/capture_surface.py` может сохранить кадр/геометрию достигнутой страницы. Он не выполняет сценарий и не даёт автоматический вердикт о всей доступности. При недоступном browser tool отметить NOT_RUN, а не подставлять screenshot макета.
+## 5. Finding и закрытие
+Находка: стабильный ID; severity; confirmed/hypothesis; поверхность; reproduction; evidence; нарушенное правило; вред; причина/гипотеза; точная переделка; acceptance. Вкусовое несогласие не объявлять нормативным дефектом. Непроверенную гипотезу не закрывать как исправленный баг.
 
-## 5. Качество опыта и производительность
-Проверь новичка без специальных знаний и повторную работу эксперта. Есть ли ясный следующий шаг, место для результата, обратимость, предсказуемая навигация, отсутствие лишнего chrome? Соответствует ли композиция качеству DS, а не просто правильным imports? Назови точную переделку, не «сделать красивее».
+Resolved требует repair record, affected files, новой проверки именно этого finding на текущем build и evidence позднее обнаружения. Ссылка на старый общий PASS не принимается. Не удалять провал из истории. Два безуспешных исправления одного root cause — повод пересмотреть причину, не снять тест.
 
-Отдельно проверь agent path: что поручено, границы, статус выполнения против статуса принятия, конкретный артефакт, журнал действий, безопасный повтор, ручной контроль. Читаемый viewer не должен вызывать acceptance.
+## Проверяемый отчёт
+`assets/assessment.example.json` — schema 2.0. Команда приведена в [формате приёмки](references/assessment-v2.md). Передай plan и независимо известные plan/app/DS hashes. `--require-ready` проверяет полноту ДАННОГО scope; не выдаёт разрешение на релиз/deploy и не доказывает правдивость автора evidence. Читай raw logs/assertions и кадры.
 
-Проверить initial JS реального приложения, lazy requests, idle animation/CPU, долгие операции, bounded loading и большие правдоподобные модели. Записать устройство/engine/data/method. Не сравнивать gzip библиотеки с whole-app budget и не выдавать одну JS-duration за FPS.
-
-## 6. Отчёт, гейты, исправления
-Каждое finding — доказательство, воспроизведение, принцип DS, причина или честная гипотеза, вред, направление исправления, acceptance и regression. Упорядочить P0→P3. В findings различать confirmed/hypothesis. Отметить положительные границы и отсутствующие проверки, не обобщать «идеально».
-
-Оформить `assessment.json`; поля структуры и состояний описаны в `assets/assessment.schema.json`. Сравнить coverage с утверждённым scope. Нельзя закрыть finding только комментариями, убрать тест или исключить сложный маршрут.
-
-```sh
-python3 SKILL_ROOT/scripts/validate_assessment.py WORK_DIR/assessment.json --evidence-root WORK_DIR --current-fingerprint CURRENT_SHA --out WORK_DIR/assessment-validation.json
-```
-Добавь `--require-ready` для CI-гейта. Валидатор требует доказательства, но не доказывает, что тест честно выполнен: проверь raw logs и код assertions. NOT_RUN/BLOCKED по required checks запрещают ready.
-
-В разрешённом fix-cycle: закрепить finding ID → исправить минимальный общий источник → добавить тест → повторить соседние варианты и обе темы → рецензировать итоговый build → закрыть с evidence. При отсутствии component/capability немедленно DS-GAP пользователю. Не подменять исправления полного UI новым дизайн-направлением.
-
-## Доставка
-Человеку: verdict для конкретного scope, главные проблемы и решения, изменения, точные limits. Следующему агенту: assessment.json, findings-report.md, fix-ledger.csv, evidence index, актуальные fingerprints, gap register и reproduction commands. Разрешение на review не даёт разрешения на deploy.
+Schema 1.0 читается только как исторический диагностический формат: он больше не выдаёт готовность. Общие dimensions обязаны быть явно applicable либо approved N/A; PASS без связанных checks недопустим. Выход — assessment, findings-report, fix-ledger, gaps, run-state и handoff, с ограничениями и следующим конкретным шагом.
