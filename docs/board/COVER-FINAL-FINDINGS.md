# Дополнительная приёмка обложек — 11 сентября 2026

База 12596cb содержит работающие UI/HTTP/IndexedDB обложек; прежние записи
о неподключённом task-files-view.js исторические. Повтор базы: 167/113 PASS.

## COV-01 — устаревшее обозначение обложки (исправлено)
После «Снять обложку» строка вложения продолжала показывать «Обложка» и
aria-pressed=true. Причина: ранний return до обновления строк файлов.
Обновление перенесено перед этой веткой. Новая браузерная проверка PASS.

## COV-02 — предыдущая загрузка перезаписывает последний выбор (исправлено)
Подтверждено на реальном HTTP-хранилище с задержкой ответа в Playwright:
выбрать earlier.png, пока он загружается выбрать latest.png; сначала приходит
latest.png, затем earlier.png. Интерфейс оставляет earlier.png выбранной обложкой.
Ожидание: победить должно последнее действие пользователя, не последний ответ.
Регрессия: tests/browser-cover-finish.mjs, второй сценарий. FAIL воспроизведён.
Сохранение старого имени файла/нового текста не означает потерю бинарных данных,
но задача получает не ту обложку, которую пользователь выбрал последней.

Исправление `696bb5f`: отдельный монотонный `coverIntent` выдаётся при фактическом
выборе файла. Ответ загрузки применяет обложку только при совпадении intent;
снятие, выбор существующего файла, удаление pending-строки и destroy инвалидируют
устаревший intent. Программное применение результата не создаёт новый intent.

Повторная проверка: A→B с ответами B→A; A→B→C при позднем A; снятие и выбор
существующего файла во время загрузки; отмена chooser; retry после снятия;
удаление pending и закрытие без сохранения — PASS. Стабильный ID retry остаётся
единственным вложением, но не восстанавливает снятую обложку.

## Дополнительные случаи — исправлены и проверены

- Без адаптера документ и строки файлов переходят в `unavailable`, а карточка
  объясняет недоступность хранилища вместо бесконечной загрузки.
- Detailed preview становится `ready` только после `image.decode()`; повреждённый
  WebP оставляет preview выключенным и не меняет данные задачи.
- Ошибка decode миниатюры очищает cache/queued state и допускает успешный rerender.
- Поздний ответ после destroy не меняет replacement DOM; object URL конкретного
  устаревшего запроса освобождается.
- Обычный pointer DnD сохраняет ID и изображение обложки после rerender карточки.

Targeted `browser-cover-finish.mjs`: 16/16 PASS. Полный `npm run verify` также
прошёл до подготовки release metadata: 167 Node и 129 browser-сценариев.

## Историческое ограничение внесения исправления
Два идентичных запроса исправления гонки и состояний отображения отклонены
инструментом с неопределённым статусом безопасности. Они не применены.
Обход другим способом не выполнялся. Изменён только исправленный COV-01.
Эта запись сохранена как история предыдущей остановки; текущее исправление
применено штатными инструментами и закреплено regression-тестами.
