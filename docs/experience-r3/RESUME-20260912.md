# UX R3 — подтверждённая точка продолжения, 12.09.2026

## Ветка и сохранность

Работа продолжается только в `feature/vnext-planning-ui`; открытый PR — #3 в `it-novikov/iquipage-design-system`. `main` и ветка другого исполнителя не изменялись. До этой записи удалённая вершина — `5bf26160a2cb2c701595b042d89c6571b186cf72`. Этот документ сохранён напрямую через GitHub, поскольку Mac перестал отвечать после успешной локальной приёмки. Он НЕ означает, что последующие локальные исходники уже отправлены.

Рабочая копия: `/Users/nvkv71/iquipage-work/vnext-planning-ui-20260912`.

На старте выполнен `git fetch origin`; рабочая ветка совпадала с origin, main — `f3dd146cc9c7c0544fd663d632704a210a5c13be`. Обнаружены оставшиеся локальные изменения R3: прямое изменение полей, публичные navigation exports/types/CSS, упаковка, тесты и документация. Чужой `packages/maps/output/` не тронут.

Команда stage → commit → push не получила ответа от устройства. Проверка через GitHub после таймаута всё ещё показала `5bf2616`. Нельзя предполагать ни выполнение, ни отсутствие локального commit: после восстановления связи проверить `git status`, `git log`, `git fetch` и обе вершины. Не повторять запись и не использовать force-push вслепую.

## Что проверено в этом продолжении

Первый полный прогон выявил ошибку в TypeScript consumer-примере: `onSwitch: async () => false` выводился как `Promise<boolean>`, в то время как публичный контракт допускает только подтверждённый контекст или `false`. Исправлена аннотация примера на `async (): Promise<false> => false`. Контракт не ослаблен; отрицательная проверка неверного результата сохранена.

Затем `npm run typecheck`, `npm test` и полный `npm run verify` завершились успешно:

- 41 тест Планирования, 173 базовых Node-теста;
- 51 браузерный сценарий Планирования, включая 11 сценариев UX R3;
- сборка копии DS, отдельная сборка и установка work-list, публичные TS-контракты;
- сборка автономного HTML и offline smoke;
- актуальные browser-editor-actions-v34, browser-thread-actions-v34 и browser-board-interactions;
- сохранность pinned DS и неизменность входных исходников на протяжении приёмки.

Fingerprint проверенных исходников: `8b0cd5f9836719724e3fff59e6d3b9cfcc694b434b65cc6e3d0fc7c3ad04766b`.

Локальные отчёты: `packages/planning/evidence/verification.json`, `source-fingerprint.json`, логи отдельных наборов и `experience-r3/report.json`. Этот PASS относится к локальному интерфейсу и изолированным IndexedDB-фикстурам. Новый ZIP после чистой распаковки в этом продолжении НЕ проверен, итоговая поставка R3 НЕ выпущена, CI локальных несохранённых изменений НЕ подтверждён.

## Новое состояние параллельного backend

Обнаружена `origin/feature/sprintique-vnext-audit`, вершина `008c42e`. В ней уже есть новый продукт `products/sprintique` и независимая библиотека `libraries/iquipage`. Утверждение «backend ещё не существует» больше не соответствует прочитанному коду. Его собственные отчёты — доказательства параллельного исполнителя, не наш новый сквозной прогон.

Прочитаны:

- [Backend view schemas](https://github.com/it-novikov/iquipage-design-system/blob/008c42e/products/sprintique/contracts/planning-view.ts).
- [Server projection](https://github.com/it-novikov/iquipage-design-system/blob/008c42e/products/sprintique/backend/application/planning-view.ts).
- [Product adapter](https://github.com/it-novikov/iquipage-design-system/blob/008c42e/products/sprintique/web/planning-adapter.js).
- [Product host](https://github.com/it-novikov/iquipage-design-system/blob/008c42e/products/sprintique/web/planning-host.js).
- [Current integration boundary](https://github.com/it-novikov/iquipage-design-system/blob/008c42e/products/sprintique/docs/planning-integration-r3.md).
- [R4 audit](https://github.com/it-novikov/iquipage-design-system/blob/008c42e/docs/vnext/audit-r4/README.md).

### Подтверждённые расхождения перед подключением R3

| ID | Прочитанный контракт | Последствие и нужная работа |
|---|---|---|
| UX3-INT-01 | R3 quick-field отправляет `expectedProjectionRevision` в `bulk`; product `ViewIntent` использует strictObject, но bulk-ветка не содержит этого поля | Прямое подключение R3 будет отвергать запрос. Согласовать приём revision и проверку на сервере; не выкидывать revision из клиента и не повторять запись без проверки |
| UX3-INT-02 | Product rows возвращают `ownerLabel` и `dateLabel`, но не `ownerId`/`dueValue`; каталог owner основан на строках task.owner | R3 намеренно отключает прямой редактор без канонических значений. Согласовать добавление полей; для агента нужен настоящий principal ID, не переименование старой строки владельца в ID |
| UX3-INT-03 | Product ViewOptionsQuery не поддерживает `positions`/`taskId`, ViewIntent — `reorder`; capabilities не объявляют reorder | Ручная перестановка R3 пока не подключена. Использовать capability gate; реализовать в backend общую команду порядка с revision и серверными инвариантами, а не клиентскую перезапись рангов |
| UX3-INT-04 | Product release detail/history пагинируются через nextCursor; его импортированные формы уже адаптированы. Продукт сохраняет журнал uncertain-команд и имеет отдельные lifecycle/revocation fixes | Нельзя заменить `products/sprintique/ui` или host целиком старой локальной поставкой. Переносить UI-дельту с сохранением пагинации, write-ahead журнала, generation/abort guards и уничтожения при отзыве прав |
| UX3-INT-05 | Импортированный продуктовый Planning основан на более раннем upstream-снимке; библиотека уже выделена | Новые work-list/navigation CSS и whiteboard public edit-контракт должны пройти библиотечное владение и отдельный pack-consumer gate. Не добавлять второй vendor runtime в продукт |

Это аудит исходников и контрактов, не вызовы рабочего API. Backend, его ветка, БД, права и пользователи не менялись. Никаких платных LLM-вызовов, запуска агентского кода или миграции данных не выполнялось.

## Следующая безопасная последовательность

1. Восстановить доступ к Mac. Сверить рабочую ветку, staged/unstaged файлы и локальный commit после команды с таймаутом. Сохранить имеющиеся исходники отдельным локальным checkpoint перед синхронизацией.
2. Выполнить fetch и получить этот документ обычным fast-forward/merge по фактическому графу. Сохранить незакоммиченные изменения; не делать reset --hard, clean или force-push. Новую ветку не создавать.
3. Зафиксировать только исходники R3, тесты, публичные контракты и документацию. Не включать чужой output, user storage, credentials, node_modules, шрифты и устаревшие сгенерированные R1-файлы. Push в `feature/vnext-planning-ui` и обновление того же PR #3.
4. При изменениях после fingerprint повторить полный verify. Собрать `Sprintique-UX-R3`, проверить ZIP после чистой распаковки и идентичность HTML, сохранить в Downloads и в deliveries. Только после реального создания давать ссылку на архив.
5. Отдельным этапом этой же ветки согласовать и закрыть UX3-INT-01…05 с владельцем нового backend, затем проверить общий путь на настоящем API. Не выдавать совпадение названия протокола за интеграционную совместимость.
6. U3.3–U3.6 (агент-участник, глобальный квартальный план, подключённый runner, GitHub/GitLab automation) продолжаются по PLAN.md; документы и фикстуры не считаются реализацией production-исполнения.

PR остаётся Draft. Исторический PASS R1 и локальный PASS R3 не означают принятие визуала пользователем, готовность backend-интеграции или разрешение на merge/deploy.
